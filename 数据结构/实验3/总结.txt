对于链表应使用结构体指针定义，因为结构体定以后给出的固定地址不可变，例如
int a;
int *b;
&a=bl;
这样的操作是不合法的，所以必须使用可以改变的指针变量
结构体中使用结构体定义后的用.引用成员，结构体指针使用->引用成员，结构体指针使用->时不需要*转义
对于链表函数创建链表时应返回结构体指针的值，这样才是将开辟的结构体空间返回，否则只是返回结构体内数据
传入函数初始化新的数据并且初始化该数据节点时，如若引入结构体改变的只是形参的值，而传入节点本身不受影响
对于链接节点的语句在执行的操作前还是操作后使用对于遍历链表时有影响